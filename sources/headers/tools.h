#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

#ifndef TOOLS 
#define TOOLS

void hexToBin(char *hex); 
unsigned char binToHex (char *bin);
int hexToDec (char *hex);

#endif // TOOLS